---

---

<div class="bg-black/70 rounded-lg shadow-md p-6 mb-6">
    <form
        action="/api/posts/create"
        method="POST"
        enctype="multipart/form-data"
        class="space-y-4"
    >
        <div>
            <textarea
                name="content"
                rows="3"
                placeholder="¿Qué estás pensando?"
                required
                class="w-full px-4 py-3 border text-white border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent resize-none"
            ></textarea>
        </div>
        <div>
            <label class="block text-sm font-medium text-white mb-2">
                Imagen (opcional)
            </label>
            <input
                type="file"
                name="image"
                accept="image/jpeg,image/png,image/gif,image/webp"
                class="w-full px-4 py-2 border text-white border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
            />
            <p class="text-xs text-gray-500 mt-1">
                Máximo 5MB. Formatos: JPG, PNG, GIF, WEBP
            </p>
        </div>
        <div class="flex justify-end">
            <button
                type="submit"
                class="bg-purple-500 text-black px-6 py-2 rounded-lg hover:bg-purple-600 transition font-medium"
            >
                Publicar
            </button>
        </div>
    </form>
</div>
